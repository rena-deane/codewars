knock(knock)()()

// below is what we get when we run knock.toString()

function (arg) {
     if (arg && arg === knock) {
        console.log('"Knock knock."');
        console.log('Whos\'s there?');
        return deliverLine;
      }
    }
